[tool.ruff]
line-length = 88
target-version = "py310"
extend-select = ["E", "W", "F", "I"]
exclude = [
    ".git",
    ".venv",
    "__pycache__",
    "venv",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
